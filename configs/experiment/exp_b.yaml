# Experiment B: compare a stateful-optimizer IMP variant and dense-warm-up
# one-shot magnitude masks after rewinding weights to initialization.
name:        "exp_b"
description: "Magnitude-mask discovery, rewind to W0, then final grokking phase"

run_one_shot_ablation: true   # also run one-shot magnitude pruning as an ablation
