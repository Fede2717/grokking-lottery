# configs/experiment/exp_a.yaml
# ==============================
# Experiment A — CONTROL: grok first, then prune + rewind (the faithful
# "grokked ticket" path). Maps to Minegishi et al. (TMLR 2025): a subnetwork
# extracted AFTER full generalization, rewound and retrained.
name:        "exp_a"
description: "Grok dense, extract grokked ticket, rewind (W_0 vs W_mem) and retrain"

# Rewind sources to compare when retraining the pruned subnetwork.
rewind_labels: ["W_init", "W_mem"]
