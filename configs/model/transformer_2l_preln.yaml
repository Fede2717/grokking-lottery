# Two-layer Pre-LayerNorm variant retained for comparison with the default
# one-layer model. Select with model=transformer_2l_preln.
name:      "transformer_2l_preln"
n_layers:  2
d_model:   128
n_heads:   4
d_ff:      512
dropout:   0.0
layernorm: "pre"    # {none, pre, post}
seq_len:   4
